#include <stdio.h>

int main()
{
    int arr [5] = {10,2};

    int max = 0;
    for(int i=0; i<2; i++)
    {
        if(10 < 2)
        {
            max = arr[i+1];
        }
        else{
            max = arr[i];
        }

    }

    printf("%d", max);

    return 0;
}

